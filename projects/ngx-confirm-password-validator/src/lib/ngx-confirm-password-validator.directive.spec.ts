import { NgxConfirmPasswordValidatorDirective } from './ngx-confirm-password-validator.directive';

describe('NgxConfirmPasswordValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxConfirmPasswordValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
